Fall 2016
SDE1
Fei Ding
Sep 12 2016

The contents of the archive:
readme.txt    /* file listing and a brief description */
prolog1.in    /* flex code */
prolog1.y     /* bison code */
prolog1.c     /* c code containing main */
yyerror.c     /* error handling routine */
build_prolog1.sh  /* buildit script */
prolog1.log   /* a log of 10 sampe uses */

To aviod compiler warnings using "gcc -Wall", using '%option' 
declarations in the first part of the flex code.
%option nounput
%option noinput

How to build and test the SDE1 ?
1. unzip the zip file
$ unzip feid-sde1.zip

2. give execute permission to the script
$ chmod +x /path/to/build_prolog1.sh

3. if build_prolog1.sh is in the current directory, just run the script
$ ./build_prolog1.sh

4. test the executable
$ ./proglog1 <filename
